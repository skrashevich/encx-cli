# iOS

Исходники приложения перенесены в **[enkapp](https://github.com/skrashevich/enkapp)**.

Сборка `Encx.xcframework` по-прежнему из корня этого репозитория:

```sh
./mobile/bind-ios.sh
# в enkapp: make framework ENCX_CLI_ROOT=/path/to/this/repo
```
